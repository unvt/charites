{
  "name": "vt-style",
  "version": "1.0.0",
  "description": "",
  "bin": {
    "vt-style": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w",
    "lint": "eslint .",
    "fix": "eslint --fix .",
    "test": "mocha -r ts-node/register test/*.ts",
    "test:watch": "npm test -- --watch --watch-files src/**/*.ts --watch-files test/**/*.ts",
    "command": "./node_modules/.bin/ts-node ./src/cli.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^8.2.0"
  },
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/commander": "^2.12.2",
    "@types/mocha": "^7.0.2",
    "@types/node": "^14.0.14",
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
